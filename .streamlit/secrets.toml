# NICDC Admin credentials
# This file is read automatically by Streamlit (st.secrets["admin_password"]).
# Shipped with the repo so the app works out of the box on Streamlit Cloud.
# To override, edit the value below OR set it in Streamlit Cloud -> Settings -> Secrets.

admin_password = "NICDC@11444"
